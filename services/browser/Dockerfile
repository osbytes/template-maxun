# Maxun does not currently publish a versioned browser image. Pin the linux/amd64
# manifest behind `latest` so template deploys remain reproducible.
FROM getmaxun/maxun-browser@sha256:748446bda2b83cd7af8ef716971b32ef8e427852802520f49335e01c74ca2355
